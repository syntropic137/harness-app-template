{
  "name": "@example/python",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "uv build",
    "typecheck": "uv run python -m compileall src tests",
    "test": "uv run pytest",
    "lint": "uv run ruff check ."
  }
}
