{
  "name": "@example/rust",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "typecheck": "cargo check",
    "test": "cargo test",
    "test:coverage": "cargo llvm-cov --package example-rust --fail-under-lines 100 --fail-under-functions 100 --fail-under-regions 100",
    "lint": "cargo clippy --all-targets -- -D warnings"
  }
}
