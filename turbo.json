{
  "$schema": "https://turborepo.dev/schema.json",
  "globalPassThroughEnv": ["CARGO_TARGET_DIR"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "target/release/**", "build/**", "bin/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build", "^typecheck"],
      "outputs": []
    },
    "lint": {}
  }
}
