# harness/sensors

Opt-in stub for the `sensors` slot. The real Rust aggregator can replace this
directory without changing the root `just sensors` recipe.

The stub exists so the shipped justfile does not delegate to a missing path.
