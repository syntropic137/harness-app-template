{
  "name": "@harness/sensors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash -n bin/sensors && node --check aggregate.mjs",
    "typecheck": "bash -n bin/sensors && node --check aggregate.mjs",
    "test": "bin/sensors --help >/dev/null && node --check aggregate.mjs",
    "lint": "bash -n bin/sensors && node --check aggregate.mjs"
  }
}
