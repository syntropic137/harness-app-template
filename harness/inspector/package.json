{
  "name": "@harness/inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check screenshot-pair.mjs && node --check record-flow.mjs && node --check keyframe-grid.mjs",
    "typecheck": "node --check screenshot-pair.mjs && node --check record-flow.mjs && node --check keyframe-grid.mjs",
    "test": "vitest run",
    "lint": "node --check screenshot-pair.mjs && node --check record-flow.mjs && node --check keyframe-grid.mjs",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0",
    "vitest": "^2.0"
  }
}
