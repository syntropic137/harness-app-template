# EXP-10 fitness-gate in practice

Claim: CobaltCoast (claude-opus-4-7), 2026-06-10, branch feat/apss-integration.
Sole-editor; do not edit this file from other agents.

## Mission slice
Trigger a fitness regression in the harness's coupling/complexity surface and
confirm the sensors-gate gives actionable, agent-readable feedback.

## Frozen hypothesis
1. `just sensors` is the front door for the fitness gate. Running it on the
   working tree (clean) produces a 0-exit baseline measurement.
2. The sensors slot computes at least coupling + complexity + abstractness
   per the files in `harness/sensors/` (adapters, abstractness, complexity).
3. Introducing a deliberately high-complexity new TypeScript module under
   `ws_apps/example-typescript/src/` and running `just sensors` will cause
   the complexity sensor to report a regression with file-level pointers an
   agent can navigate.
4. The verdict shape is JSON or a structured table; not freeform text.
5. The gate is advisory unless a budget is set. If thresholds live in
   `harness/sensors/baseline.json`, an artificial regression bigger than the
   tolerance will exit non-zero.

## Frozen prediction
- 1 PASS: `just sensors` exits 0 on clean tree (no regression vs baseline).
- 2 PASS: sensors module computes at least coupling, complexity, abstractness.
- 3 PASS: a deliberately complex file triggers a regression report.
- 4 PASS: structured output (JSON or table), agent-readable.
- 5 PASS: hard exit only when tolerance exceeded.

Combined verdict prediction: CONFIRMED. The fitness story is on track even
though EXP-08 surfaced docs gaps elsewhere.

## Probes
- `just sensors --help` and `just sensors` to baseline output.
- Read `harness/sensors/baseline.json` for current per-signal mean.
- Add `ws_apps/example-typescript/src/_fitness_smoke.ts` with a
  deliberately deep nested if-else ladder (high cyclomatic complexity); rerun
  `just sensors`; capture exit code + output diff.
- Remove the file, rerun, confirm clean state.

## Reusable empirical claims (to be filled in verdict)
- TBD with N=1 evidence count.

## Friction items (append to FRICTION.md on conclusion)
- TBD.
